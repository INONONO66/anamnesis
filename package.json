{
  "name": "anamnesis-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun scripts/ensure-test-db.ts && bun test packages",
    "typecheck": "tsc --build",
    "schemas": "bun run packages/protocol/scripts/export-schemas.ts",
    "test:db": "bun scripts/ensure-test-db.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21",
    "neo4j-driver": "^6.2.0",
    "typescript": "^5.9.2"
  }
}
