{
  "name": "anamnesis-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun scripts/qa/runtime-scenarios.ts --case foundation --evidence-root .omo/evidence/foundation/root-test",
    "typecheck": "tsc --build",
    "schemas": "bun run packages/protocol/scripts/export-schemas.ts",
    "test:db": "bun scripts/ensure-test-db.ts",
    "migrate": "bun scripts/migrate.ts",
    "backup": "bun scripts/backup.ts",
    "db:up": "docker compose up -d neo4j",
    "build:runtime": "bun build app/anamnesis/main.ts --target=node --outfile=dist/anamnesis-daemon.mjs && bun build app/anamnesis/ops.ts --target=node --outfile=dist/anamnesis-ops.mjs && bun build app/anamnesis/client.ts --target=node --outfile=dist/anamnesis-client.mjs && mkdir -p dist/prompts && cp app/anamnesis/prompts/extract-claims.v2.md dist/prompts/extract-claims.v2.md && cp app/anamnesis/prompts/judge-relations.v1.md dist/prompts/judge-relations.v1.md",
    "test:runtime": "bun run build:runtime && node --test app/anamnesis/process.test.mjs",
    "test:runtime:surface": "node app/anamnesis/surface.mjs"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21",
    "neo4j-driver": "^6.2.0",
    "typescript": "^5.9.2"
  }
}
