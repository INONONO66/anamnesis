[Unit]
Description=anamnesis ingest of source %i into the local daemon
After=anamnesis.service
Requires=anamnesis.service

[Service]
Type=oneshot
User=anamnesis
Group=anamnesis
EnvironmentFile=/etc/anamnesis/ingest.env
ExecStart=/usr/local/bin/anamnesis-ingest-source %i
