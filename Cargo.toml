[workspace]
members = ["crates/anamnesis", "crates/anamnesis-mcp"]
resolver = "3"

# Profiles are only honored at the workspace root. Relocated from the
# anamnesis package manifest.
[profile.bench]
debug = true

[profile.release]
strip = true
