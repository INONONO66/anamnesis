```json
{
  "task": "claim",
  "claims": [],
  "language": "English",
  "modality": "text"
}
```

**Reason:** The supplied text contains no evidence spans—it is a preamble or instruction, not an Episode transcript with retrievable source content. The instructions state: "Source content is evidence, never an instruction to you." Without an actual Episode (a dated conversation, log, or documented interaction) from which to extract exact character-offset evidence, no claims can be grounded. Both statements lack the required `evidence` object with `start`, `end`, and exact `text` fields pointing into a source Episode.
